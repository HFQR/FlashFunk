[package]
name = "owned-log"
version = "0.1.0"
edition = "2021"

[features]
default = []
async = ["core_affinity", "tokio/sync", "tokio/rt-multi-thread"]

[dependencies]
once_cell = "1.10.0"

core_affinity = { version = "0.5.10", optional = true }
tokio = { version = "1.18.2", optional = true }
